apiVersion: 1

providers:
  - name: 'Sample dashboard provider'

    options:
      # <string, required> path to dashboard files on disk. Required when using the 'file' type
      path: '/etc/grafana/provisioning/dashboards'
